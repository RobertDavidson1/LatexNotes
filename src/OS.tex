% Load the tcolorbox package with required options
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage[most]{tcolorbox}
\usepackage{xcolor}
\usepackage{geometry}

% Define custom colors for a sleek, modern look
\definecolor{definitionbg}{RGB}{248, 249, 250}
\definecolor{definitionborder}{RGB}{42, 59, 87}
\definecolor{definitiontitle}{RGB}{42, 59, 87}

% Define a modern, sleek definition box
\newtcolorbox{definition}[2][]{%
  enhanced,
  breakable,
  colback=definitionbg,
  colframe=definitionborder,
  arc=0pt, % Sharp corners for modern look
  outer arc=0pt,
  leftrule=2pt, % Only left border is thick
  rightrule=0.5pt,
  toprule=0.5pt,
  bottomrule=0.5pt,
  left=10pt,
  right=10pt,
  top=8pt,
  bottom=8pt,
  toptitle=3pt,
  bottomtitle=3pt,
  fonttitle=\sffamily\bfseries\color{white},
  coltitle=white,
  colbacktitle=definitiontitle,
  title={\sffamily\textsc{Definition} \normalfont#2},
  overlay unbroken and first={
    \node[anchor=west, font=\sffamily\small\bfseries, text=definitionborder, 
          xshift=10pt, yshift=-8pt] at (frame.north west) {};
  },
  #1
}

% Usage example
\begin{document}

\begin{definition}{Continuous Function}
    A function $f: X \rightarrow Y$ between topological spaces is said to be \textbf{continuous} if for every open set $V \subseteq Y$, the preimage $f^{-1}(V)$ is an open set in $X$.

    Equivalently, a function $f: \mathbb{R} \rightarrow \mathbb{R}$ is continuous at a point $c$ if for every $\varepsilon > 0$, there exists a $\delta > 0$ such that
    \begin{align}
        |x - c| < \delta \implies |f(x) - f(c)| < \varepsilon
    \end{align}
    for all $x$ in the domain of $f$.
\end{definition}

\end{document}